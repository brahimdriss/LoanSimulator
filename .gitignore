.venv/
*.pyc
*.pkl
# *.csv
# *.pt
# *.log
auto_push.log
*.json

stable/

# Exclude identity-revealing and large output files
*.sh
# ...but the cluster submission wrapper must be versioned: it is the file
# HTCondor executes, and both agents' identical config lives inside it.
!cluster/*.sh
*.out
*.err
results_*/
weights_*/.DS_Store

# local plotting/smoke-test venvs (this Mac only)
.plot-venv*/
# raw campaign data pulled from the cluster (not versioned; the rendered plots are)
run11_data/
ablation_data/
